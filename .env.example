# 数据库配置
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=timezone_demo
DB_SSLMODE=disable
DB_TIMEZONE=UTC

# 应用配置
PORT=8080
GIN_MODE=release
LOG_LEVEL=info

# 时区配置
TZ=UTC

# 可选：Redis 配置（如果启用缓存）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# 可选：监控配置
METRICS_ENABLED=false
METRICS_PORT=9090

# 开发环境配置
# GIN_MODE=debug
# LOG_LEVEL=debug